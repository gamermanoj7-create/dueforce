# DUEFORCE V17 — Mobile Navigation Fix

V17 keeps the V16 production/legal/PDF build and hardens mobile navigation using direct global navigation, pointer/touch-safe controls, and explicit view activation.

Deploy:
`firebase use dueforce-926bb`
`firebase deploy --only hosting,firestore:rules`
